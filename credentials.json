{
    "android": {
      "keystore": {
        "keystorePath": "android/app/upload-keystore.jks",
        "keystorePassword": "KEYSTORE_PASSWORD",
        "keyAlias": "keytool -genkey -v -keystore my-upload-key.keystore -alias my-app-alias -keyalg RSA -keysize 2048 -validity 10000",
        "keyPassword": "KEY_PASSWORD"
      }
    }
  }